//////////////////////////////////////////////////////////////////////////////////////////////////
//////////////////////////1 The developer thought the company is named Fruits & Bananas Corp, but it's actually called Fruits & Vegetables Corp
//////////////////////////////////////////////////////////////////////////////////////////////////
document.querySelector('title').textContent = "Fruits & Vegetables Corp";
//////////////////////////////////////////////////////////////////////////////////////////////////
//////////////////////////2 Text content in header h1 is wrong, should be Fruits & Vegetables Corp (see first issue)
//////////////////////////////////////////////////////////////////////////////////////////////////
document.querySelector('header h1').textContent = "Fruits & Vegetables Corp";
//////////////////////////////////////////////////////////////////////////////////////////////////
////3 The last a tag in header ul has wrong text content and href attribute (should be Vegetables and not Bananas) 
//////////////////////////////////////////////////////////////////////////////////////////////////

let findLastATag = document.querySelector('ul li:last-child a');

findLastATag.setAttribute('href', '#vegetables');

findLastATag.innerHTML = "Vegetables";


//////////////////////////////////////////////////////////////////////////////////////////////////
////4 The section #contact and #about are in the wrong order. Swap them
//////////////////////////////////////////////////////////////////////////////////////////////////

let sections = document.querySelectorAll('section');
console.log(sections);
let main = document.querySelector('main');

for (let i = 0; i < sections.length; i++) {
    console.log(sections[i]);
    main.insertBefore(about, contact);
}
about.textContent = "";
//////////////////////////////////////////////////////////////////////////////////////////////////
////5 Each section should have a h2 tag at the top with corresponding text according to its id
//////////////////////////////////////////////////////////////////////////////////////////////////


let aboutSection = document.querySelector('#about');
let swapAbout = document.createElement("h2");
swapAbout.textContent = "About";
aboutSection.appendChild(swapAbout);


let contactSection = document.querySelector('#contact');
let swapContact = document.createElement("h2");
swapContact.textContent = "Contact";
contactSection.appendChild(swapContact);
contactSection.insertBefore(swapContact, contactSection.querySelector("p"));
//////////////////////////////////////////////////////////////////////////////////////////////////
////6 The text "We're the best in fruits & vegetables" under #about should be wrapped in a p tag
//////////////////////////////////////////////////////////////////////////////////////////////////

let pTag = document.createElement('p');
aboutSection.appendChild(pTag);
pTag.textContent = ("We're the best in fruits & vegetables");
//////////////////////////////////////////////////////////////////////////////////////////////////
////7 The developer used td elements in thead instead of th. Fix it.
//////////////////////////////////////////////////////////////////////////////////////////////////

let td = document.querySelectorAll("thead td");
for (let i = 0; i < td.length; i++) {
    let th = document.createElement("th");
    th.textContent = td[i].textContent;
    td[i].replaceWith(th);
}

//////////////////////////////////////////////////////////////////////////////////////////////////
////8 The developer forgot to include the main.css file. Add it to head
//////////////////////////////////////////////////////////////////////////////////////////////////

let head = document.querySelector('head');
let link = document.createElement('link');
link.type = 'text/css';
link.rel = 'stylesheet';
link.href = "main.css";
head.appendChild(link);
